# Dependencies
node_modules/

# Sensitive financial documents — NEVER commit real bank statements
*.pdf

# Photos / images (event pics, personal) — the app stores images in Firestore, not as files
*.jpg
*.jpeg
*.png
*.gif
*.heic

# Exception: icones de l'app (PWA). Ce sont des assets generes, pas des photos,
# et elles doivent etre publiees pour que l'installation sur telephone marche.
!icon-192.png
!icon-512.png
!icon-maskable-512.png

# OS / tooling artifacts
nul
.DS_Store
Thumbs.db

# Editor / agent local config
.claude/

# Subproject with its own tooling
flemme-gym/
